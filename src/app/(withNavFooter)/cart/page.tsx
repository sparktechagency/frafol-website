import CartSection from "@/components/Cart/CartSection";
import React from "react";

const page = () => {
  return (
    <main className="py-10">
      <CartSection />
    </main>
  );
};

export default page;
