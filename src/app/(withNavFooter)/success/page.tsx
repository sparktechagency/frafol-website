import PaymentSuccess from '@/components/shared/PaymentSuccess';
import React from 'react';

const page = () => {
    return (
        <div>
            <PaymentSuccess />

        </div>
    );
};

export default page;